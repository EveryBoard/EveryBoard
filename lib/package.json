{
  "name": "@everyboard/lib",
  "version": "1.0.0",
  "description": "EveryBoard's library",
  "main": "index.js",
  "private": "true",
  "scripts": {
    "build": "tsc --build",
    "watch": "tsc --watch",
    "test": "nyc -x '**/*.spec.ts' npx jasmine --config=jasmine.json",
    "coverage": "nyc --branches 100 --lines 100 --statements 100 --functions 100 check-coverage"
  },
  "devDependencies": {
    "istanbul": "^0.4.5",
    "jasmine": "^5.1.0",
    "ts-node": "^10.9.1"
  }
}
