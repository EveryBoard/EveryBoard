import { Localized } from 'src/app/utils/LocaleUtils';

export class DraughtFailure {

    public static readonly CANNOT_GO_BACKWARD: Localized = () => $localize`You cannot go backward with normal pieces!`;

}
