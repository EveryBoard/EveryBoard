import { GameState } from 'src/app/jscaip/GameState';

/**
  * This class represent a board at a mentionned turn
  * If your game's board use square spaces in a limited rectangular board (with or without holes) use GameStateWithTable
  * If your game's pieces are hexagonal pieces in a limited space, use HexagonalGameState
  * If your game's pieces are hexagonal but the board is not confined to a fix area, use OpenHexagonalState
  * If your game's pieces are triangular pieces in a limited space use TriangularGameState
  */
export class NewGameState extends GameState {

}
