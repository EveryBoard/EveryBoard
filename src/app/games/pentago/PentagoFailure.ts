export class PentagoFailure {

    public static readonly CANNOT_ROTATE_NEUTRAL_BLOCK: string = $localize`If the block to turn is neutral, please just use move without rotation.`;

    public static readonly MUST_CHOOSE_BLOCK_TO_ROTATE: string = $localize`No block is neutral, you must pick a block to rotate.`;
}
