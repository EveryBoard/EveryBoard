export class AwaleFailure {
    public static CANNOT_DISTRIBUTE_FROM_ENEMY_HOME: string = $localize`You cannot distribute from the opponent's home.`;

    public static MUST_CHOOSE_NONEMPTY_HOUSE: string = $localize`You should choose a non-empty house to distribute.`

    public static SHOULD_DISTRIBUTE: string = $localize`You should distribute but you do not.`
}
