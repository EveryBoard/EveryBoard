export const environment = {
    useEmulators: true,
    emulatorConfig: {
        projectId: 'my-project',
        databaseURL: 'http://localhost:8080',
    },
    production: true,
    devPages: true,
    test: false,
    root: '/',
};
